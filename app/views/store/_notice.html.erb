<% if notice.present? %>
    <p class="py-2 px-3 bg-gray-50 mb-5 text-black-500 font-medium rounded-lg inline-block" id="notice">
    <%= notice %>
    </p>
<% else %>
    <div id='notice'></div>
<% end %>